import styled from 'styled-components';

export const FilterSection = styled.div`
	display: flex;
	flex-wrap: wrap;
	justify-content: space-between;
	align-items: center;
`;
export const Select = styled.article`
// background-color: ${(props) => props.background};
width: ${(props) => props.width};
fullWidth: true;
`;
